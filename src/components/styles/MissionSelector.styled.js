import styled from "styled-components"
import { MissionSelector } from "../MissionController/MissionSelector"

export const StyledMissionSelector = styled(MissionSelector)`

   border-radius: 8px;
   padding: 10px 15px;
   width: 55%;

   &, > * {
      color: black;
   }

`